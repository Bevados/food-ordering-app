/**
 * Интерфейс для пропсов компонента HeaderCartButton.
 *
 * @property {() => void} onClick - Функция-обработчик, вызываемая при клике на кнопку, открывает корзину.
 */
export interface HeaderCartButtonProps {
	onClick: () => void
}
